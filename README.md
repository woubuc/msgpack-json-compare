# Compare JSON & msgpack

Paste your JSON data in this tool and see how much smaller msgpack can make it.

## Development
- Install dependencies: `yarn install`
- Run development server: `yarn dev`
- Build static bundle: `yarn build`
